{% extends 'baseBack.html.twig' %}

{% block title %}Livreur{% endblock %}

{% block body %}
    {%  include "partials/backHeader.html.twig"%}
<div class="container d-flex flex-column align-items-center">
    <h1>Livreur</h1>

    <table class="table table-bordered" >
        <tbody>
            <tr>
                <th style="">Id</th>
                <td>{{ livreur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ livreur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ livreur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ livreur.email }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ livreur.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ livreur.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_livreur_index') }}" class="btn btn-fill-out btn-block">back to list</a>

    <a href="{{ path('app_livreur_edit', {'id': livreur.id}) }}" class="btn btn-fill-out btn-block">edit</a>

    {{ include('livreur/_delete_form.html.twig') }}
</div>
{% endblock %}
